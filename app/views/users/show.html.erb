	
	<section class="">
		<div class="flex flex-col justify-center items-center">
        <div class="mx-auto flex h-28 w-28 flex-shrink-0 items-center justify-center rounded-full bg-red-100 sm:mx-0 overflow-hidden">
				<img src=<%= @user.avatar %> class="overflow-hidden" />
            </div>
		<h3 class="text-xl font-bold mt-4 mb-0"><%= @user.username&.upcase %></h3>
		<p class="text-sm text-gray-500 "><%= @user.email %></p>
	<%  if is_loggedin_user?(@user.id) %>
		  <%= link_to "Edit", edit_user_path(@user), class: "mt-3 inline-flex w-full justify-center rounded-md border border-gray-300 bg-white px-4 py-2 text-base font-medium text-gray-700 shadow-sm hover:bg-gray-50 focus:outline-none focus:ring-2 focus:ring-indigo-500 focus:ring-offset-2 sm:ml-3 sm:w-auto sm:text-sm mt-2 "  %>
		  <%= button_to "Delete", @user, method: :delete,  form: {data: { turbo_confirm: "Are you sure you want to delete this user"} } , class: "mt-3 inline-flex w-full justify-center rounded-md border border-gray-300 bg-red-500 px-4 py-2 text-base font-medium t shadow-sm  focus:outline-none focus:ring-2 focus:ring-indigo-500 focus:ring-offset-2 sm:ml-3 sm:w-auto sm:text-sm mt-2 !text-white "  %>
	<% end %>
		</div>
	<div class="flex gap-5 mb-10 flex-wrap mt-10">
	
		<% @user.articles.each do |a| %>
		<%= render "articles/components/article_card", article: a %>
<% end  %>
</div>
	</div>


	
	</section>
